Canonical train/validation/test index files used by all benchmark runs.

The `.npy` files are derived from the fixed stratified split seed 42 package and
match the 64/16/20 split used throughout the benchmark.
