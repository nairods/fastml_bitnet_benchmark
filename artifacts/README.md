Contains the fitted `StandardScaler` object used by the benchmark pipeline.

It is generated from the stored split statistics, so the repo can reproduce the
exact preprocessing step without manual reconstruction.
